steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - build
      - -f
      - usr/Jobs/event-recap/Dockerfile
      - -t
      - us-central1-docker.pkg.dev/partyscene-441317/scenes-backstage/event-recap
      - .   # build context = repo root, so COPY can reach shared/

images:
  - us-central1-docker.pkg.dev/partyscene-441317/scenes-backstage/event-recap
